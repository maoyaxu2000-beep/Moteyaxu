# 把「空保英语」装到手机上 —— 详细教程

> 这份应用已经做好了，就在「空保英语App」这个文件夹里。
> 下面两种方法任选一种。**强烈建议先用方法一**，最简单，五分钟搞定，什么都不用安装。

---

## 方法一：添加到主屏幕（最简单，推荐）⭐

做完后，手机桌面会多出一个「空保英语」图标，点开就是全屏 App，能离线用。

### 第 1 步：把文件夹传到手机
把电脑上的整个 **「空保英语App」文件夹** 传到手机，任选一种：
- 用 **微信**：电脑上打开「文件传输助手」，把文件夹压缩成 zip 发到手机，手机里解压；
- 或用 **数据线** 连电脑，把文件夹拷到手机存储里；
- 或上传到 **百度网盘 / QQ** 等，再用手机下载。

> 传的时候要保证文件夹里这几个文件都在：`index.html`、`app.js`、`data.js`、`icon.svg`、`manifest.webmanifest`、`sw.js`。

### 第 2 步：用浏览器打开 index.html
- **安卓手机**：用 **Chrome** 或 **夸克 / 手机自带浏览器**，打开文件夹里的 `index.html`（在“文件管理”里找到它，点开，选择用浏览器打开）。
- **苹果 iPhone**：用 **Safari** 浏览器打开 `index.html`（可先把文件放到“文件”App，再用 Safari 打开）。

打开后你就能看到完整的应用界面了。

### 第 3 步：添加到主屏幕
- **安卓 / Chrome**：点右上角 **⋮**（三个点）→ 选 **「添加到主屏幕」** 或 **「安装应用」** → 确认。
- **苹果 / Safari**：点底部中间的 **分享按钮（方框带向上箭头 ⬆️）** → 往下滑找到 **「添加到主屏幕」** → 点右上角「添加」。

完成！现在桌面上有「空保英语」图标了，点开即用，**断网也能用**，学习进度和打卡都存在手机本地。

---

## 方法二：做成真正的安装包 APK（进阶，安卓专用）

如果你一定要一个能像普通软件那样安装的 `.apk` 文件，用 **PWABuilder**（一个免费网站，全程点鼠标，不用写代码）。它需要先把应用放到一个网址上。

### 第 1 步：把应用免费放到网址上
推荐用 **Netlify**（免费）：
1. 电脑浏览器打开 **app.netlify.com**，用邮箱免费注册并登录。
2. 登录后找到 **「Add new site」→「Deploy manually」**（手动部署）。
3. 把整个 **「空保英语App」文件夹** 直接 **拖进** 页面里的上传框。
4. 等几秒，它会给你一个网址，类似 `https://xxxx.netlify.app`。在手机上打开这个网址能正常显示，就说明成功了。

> **用 GitHub 托管也可以（和 Netlify 二选一）**，步骤见下面「附：用 GitHub Pages 托管」。

### 第 2 步：用 PWABuilder 生成 APK
1. 电脑浏览器打开 **www.pwabuilder.com**。
2. 把上一步那个 `https://xxxx.netlify.app` 网址粘进去，点 **Start / 开始**。
3. 它会分析你的应用，然后点 **「Package For Stores」→ 选「Android」→「Generate Package」**。
4. 下载下来的压缩包里有一个 **`.apk`** 文件（通常叫 `app-release-signed.apk` 之类）。

### 第 3 步：在安卓手机上安装 APK
1. 把这个 `.apk` 传到安卓手机。
2. 点开它安装。第一次会提示「禁止安装未知来源应用」，按提示去 **设置里允许这个浏览器/文件管理器安装应用**，再回来点安装即可。

> 注意：iPhone（苹果）系统不允许这样安装 APK，苹果用户请用 **方法一**。
> PWABuilder 也能生成上架 Google Play 的包，但上架需要开发者账号，自用不需要。

---

## 附：用 GitHub Pages 托管（免费，给你一个 https 网址）

这一步是「方法二第 1 步」的替代方案。做完后你会得到一个网址，既能直接「添加到主屏幕」，也能拿去 PWABuilder 生成 APK。

1. **注册**：电脑打开 **github.com**，用邮箱免费注册并登录。
2. **新建仓库**：点右上角 **「+」→「New repository」**。
   - Repository name 填一个英文名，例如 `kongbao-english`；
   - 选 **Public（公开）**；
   - 点最下面 **「Create repository」**。
3. **上传文件**：进入刚建好的仓库页面，点 **「Add file」→「Upload files」**。
   - 把 **「空保英语App」文件夹里面的文件全部** 拖进去（注意是里面的 `index.html`、`app.js`、`data.js`、`icon.svg`、`manifest.webmanifest`、`sw.js`，**不要**把外层文件夹整个拖进去，要让 `index.html` 直接在仓库的最外层）；
   - 拖完后点下方绿色 **「Commit changes」**。
4. **开启 Pages**：点仓库上方 **「Settings」→** 左边菜单 **「Pages」**。
   - 「Source」选 **Deploy from a branch**；
   - 「Branch」选 **main**，文件夹选 **/(root)**，点 **「Save」**。
5. **拿到网址**：等 1～2 分钟，刷新这个 Pages 页面，上面会出现一个网址，类似
   `https://你的用户名.github.io/kongbao-english/`
   手机浏览器打开它能正常显示，就成功了。
   - 想直接当 App 用：在手机上打开这个网址，按 **方法一第 3 步**「添加到主屏幕」。
   - 想做 APK：把这个网址拿去 **方法二第 2 步** 的 PWABuilder。

> 小提示：如果打开是白屏，多半是文件没放在仓库最外层（`index.html` 必须能直接点到）。重新按第 3 步上传即可。
> GitHub 偶尔在国内访问较慢，多刷新或换个网络一般就好。

## 常见问题

- **点了没声音？** 发音用的是手机自带的英文语音，第一次可能要在手机「设置 → 语言/朗读」里确认装了英文语音；按一下喇叭🔊按钮即可朗读。
- **数据会丢吗？** 打卡、生词本、已学单词都存在手机本地，正常使用不会丢；清理浏览器数据或卸载会清掉。
- **打不开 / 白屏？** 确认 `index.html`、`app.js`、`data.js` 三个文件在同一个文件夹里、没有漏传。

需要我帮你把方法二里某一步再拆细、或截图级别地讲，随时说。
